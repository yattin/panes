- 所有脚本均使用 `fileURLToPath(import.meta.url)` 动态计算脚本所在目录及仓库根路径，确保跨环境路径解析的稳定性。
- 命令行参数解析采用手动遍历 `process.argv` 的方式，支持位置参数与命名选项（如 `--output`、`--repo`）的组合。
- 共享逻辑抽取至 `scripts/lib/` 目录下的 `.mjs` 文件中，并通过相对路径导入，保持顶层脚本的简洁性。