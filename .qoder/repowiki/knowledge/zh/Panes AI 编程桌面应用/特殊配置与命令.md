- **统一构建**：根目录 `package.json` 编排了从前端构建到 Tauri 打包的全流程（如 `build:desktop`）。
- **版本同步**：通过 `scripts/sync-versions.mjs` 确保 Rust `Cargo.toml`、前端 `package.json` 及 Homebrew Cask 版本号的一致性。
- **Sidecar 编译**：使用 `build-claude-sidecar.mjs` 独立编译并分发 Node.js 侧车脚本。