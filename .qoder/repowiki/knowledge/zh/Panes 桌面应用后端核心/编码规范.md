- 所有耗时或阻塞操作（如数据库查询、Git 命令执行）均通过 `tokio::task::spawn_blocking` 或 `tauri::async_runtime::spawn` 异步执行，避免阻塞 UI 线程。
- 错误处理统一使用 `anyhow::Result` 进行传播，并在边界处通过 `.context()` 添加语义化错误信息。
- 模块内部通过 `mod.rs` 统一导出子模块，外部引用时使用 `crate::module::submodule` 的绝对路径风格。