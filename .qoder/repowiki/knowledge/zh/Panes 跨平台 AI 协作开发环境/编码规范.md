- 前后端均采用 `application/domain/infrastructure` 分层模式组织业务逻辑，确保领域规则与基础设施解耦。
- IPC 通信遵循严格的类型映射约定，前端 `types.ts` 与后端 `models.rs` / `serde` 结构保持字段名与语义的一致性。
- 异步任务在后端通过 `tokio::spawn` 或 `spawn_blocking` 托管，避免阻塞主线程，并在 `AppState` 中通过 `Arc` 共享所有权。