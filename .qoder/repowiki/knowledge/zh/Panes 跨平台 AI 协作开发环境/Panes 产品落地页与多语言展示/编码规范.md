- 在 `i18n.js` 中采用命名空间化的 JSON 结构（如 `hero.titleHtml`, `common.download`）组织多语言词条，并通过点号路径进行动态检索。
- 在 `index.html` 的模拟组件中统一使用 `data-view` 属性驱动视图状态切换，配合 CSS 类名控制显隐，实现无框架的交互逻辑。
- 在 `style.css` 中广泛使用 CSS 自定义属性（Design Tokens）定义颜色、间距与字体，确保深色主题下视觉风格的一致性。